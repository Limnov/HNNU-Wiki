# 编程术语

> 本页面介绍编程中最常见的术语和概念，帮助你建立正确的编程思维。

## 🎯 为什么要理解编程术语？

**新手常见困惑：**
- "什么是变量？为什么要声明类型？"
- "函数和方法有什么区别？"
- "库和框架是一回事吗？"

**理解术语的重要性：**
- ✅ 阅读文档不懵
- ✅ 与他人交流顺畅
- ✅ 理解设计思想
- ✅ 避免概念混淆

---

## 📦 数据和变量

### 数据类型

**定义**：数据的种类

**常见数据类型：**

#### 整数（Integer）
- **定义**：没有小数部分的数字
- **例子**：`1`, `100`, `-5`
- **用途**：计数、索引

#### 浮点数（Float）
- **定义**：带小数部分的数字
- **例子**：`3.14`, `0.01`, `-2.5`
- **用途**：计算、测量

#### 字符串（String）
- **定义**：文本数据
- **例子**：`"Hello World"`, `"你好"`
- **用途**：文本处理

#### 布尔值（Boolean）
- **定义**：真或假
- **例子**：`True`, `False`
- **用途**：逻辑判断

### 变量

**定义**：存储数据的容器

**比喻**：
- 变量 = 有标签的盒子
- 变量名 = 标签
- 变量值 = 盒子里的东西

**示例（Python）：**
```python
age = 20          # 变量名是age，值是20
name = "张三"     # 变量名是name，值是"张三"
is_student = True # 变量名是is_student，值是True
```

**变量命名规则：**
- 只能包含字母、数字、下划线
- 不能以数字开头
- 区分大小写
- 不能用保留字

### 常量

**定义**：不会改变的值

**示例：**
```python
PI = 3.14159      # 圆周率
MAX_SIZE = 100    # 最大值
```

---

## 🎭 函数和方法

### 函数（Function）

**定义**：可重复使用的代码块

**基本结构：**
```python
def add(a, b):      # 函数名和参数
    return a + b    # 返回结果

result = add(3, 5)  # 调用函数
```

**组成部分：**
- **函数名**：函数的名称
- **参数**：输入数据
- **函数体**：执行的代码
- **返回值**：输出数据

**为什么需要函数？**
- 避免重复代码
- 提高代码复用性
- 让代码更清晰

### 方法（Method）

**定义**：属于对象或类的函数

**函数 vs 方法：**
```python
# 函数（不属于任何类）
def greet(name):
    return f"Hello, {name}!"

# 方法（属于类）
class Person:
    def greet(self, name):  # self 表示当前对象
        return f"Hello, {name}!"
```

---

## 🏗️ 程序结构

### 顺序结构

**定义**：代码从上到下依次执行

**示例：**
```python
print("第一")
print("第二")
print("第三")
# 输出：第一 → 第二 → 第三
```

### 选择结构（条件语句）

**定义**：根据条件选择执行

**示例：**
```python
age = 18

if age >= 18:
    print("成年人")
else:
    print("未成年人")
```

### 循环结构

**定义**：重复执行代码

**for循环：**
```python
for i in range(5):
    print(i)
# 输出：0 1 2 3 4
```

**while循环：**
```python
count = 0
while count < 5:
    print(count)
    count += 1
```

---

## 🧩 数据结构

### 数组（Array）

**定义**：按顺序存储的数据集合

**特点：**
- 固定大小（大多数语言）
- 通过索引访问
- 连续内存

**示例：**
```python
# Python中的列表（类似数组）
numbers = [1, 2, 3, 4, 5]
print(numbers[0])  # 输出：1
```

### 列表（List）

**定义**：动态大小的数组

**特点：**
- 可变大小
- 可存储不同类型
- 提供丰富方法

**示例：**
```python
fruits = ["苹果", "香蕉", "橙子"]
fruits.append("葡萄")  # 添加元素
```

### 字典（Dictionary）

**定义**：键值对集合

**特点：**
- 通过键访问值
- 无序（Python 3.7+有序）
- 快速查找

**示例：**
```python
person = {
    "name": "张三",
    "age": 20,
    "city": "北京"
}
print(person["name"])  # 输出：张三
```

### 集合（Set）

**定义**：不重复元素的无序集合

**特点：**
- 自动去重
- 无序
- 快速查找

**示例：**
```python
numbers = {1, 2, 3, 3, 4}
print(numbers)  # 输出：{1, 2, 3, 4}
```

---

## 🎨 面向对象编程

### 类（Class）

**定义**：对象的模板/蓝图

**示例：**
```python
class Person:
    def __init__(self, name, age):
        self.name = name
        self.age = age

    def say_hello(self):
        print(f"我是{self.name}")
```

### 对象（Object）

**定义**：根据类创建的实例

**示例：**
```python
person1 = Person("张三", 20)  # 创建对象
person1.say_hello()              # 调用方法
```

### 类 vs 对象

| 特性 | 类 | 对象 |
|------|---|------|
| **比喻** | 模具 | 产品 |
| **例子** | "人类" | "张三" |
| **数量** | 一个 | 多个 |

### 面向对象的特性

#### 封装（Encapsulation）

**定义**：隐藏内部实现细节

**好处：**
- 保护数据
- 降低复杂度
- 便于维护

#### 继承（Inheritance）

**定义**：子类拥有父类的属性和方法

**示例：**
```python
class Animal:
    def eat(self):
        print("吃东西")

class Dog(Animal):  # Dog继承Animal
    def bark(self):
        print("汪汪")
```

#### 多态（Polymorphism）

**定义**：同一方法，不同实现

**示例：**
```python
class Animal:
    def speak(self):
        pass

class Dog(Animal):
    def speak(self):
        return "汪汪"

class Cat(Animal):
    def speak(self):
        return "喵喵"
```

---

## 🔄 编程范式

### 编程语言分类

#### 按运行方式

**编译型：**
- C、C++、Go
- 先编译，后执行
- 速度快

**解释型：**
- Python、JavaScript、Ruby
- 边解释边执行
- 开发快

#### 按类型系统

**静态类型：**
- C、C++、Java
- 声明类型
- 类型安全

**动态类型：**
- Python、JavaScript、Ruby
- 自动推断类型
- 灵活

#### 按编程思想

**面向过程：**
- C、Pascal
- 以过程/函数为中心
- 适合小项目

**面向对象：**
- Java、C++、Python
- 以对象为中心
- 适合大项目

**函数式：**
- Haskell、Lisp、F#
- 以函数为中心
- 无副作用

---

## 📚 代码组织

### 模块（Module）

**定义**：包含Python代码的文件

**示例：**
```python
# mymodule.py
def greet(name):
    return f"Hello, {name}!"

# 使用
import mymodule
mymodule.greet("张三")
```

### 包（Package）

**定义**：包含多个模块的目录

**示例：**
```
mypackage/
├── __init__.py
├── module1.py
└── module2.py
```

### 库（Library）

**定义**：可复用的代码集合

**比喻**：工具箱

**示例：**
- NumPy（数学库）
- Requests（HTTP库）
- Matplotlib（绘图库）

### 框架（Framework）

**定义**：提供应用骨架的半成品软件

**比喻**：房子的毛坯

**示例：**
- Django（Web框架）
- Flask（轻量级Web框架）
- Vue（前端框架）

---

## 🔧 高级概念

### 接口（Interface）

**定义**：定义行为的规范

**作用：**
- 解耦合
- 多态
- 约束实现

### 抽象类（Abstract Class）

**定义**：不能被实例化的类

**作用：**
- 定义通用接口
- 强制子类实现方法

### 设计模式

**定义**：解决特定问题的经典方案

**常见设计模式：**
- **单例模式**：一个类只有一个实例
- **工厂模式**：创建对象的工厂
- **观察者模式**：事件监听
- **策略模式**：算法族

---

## 🎯 编程流程

### 开发流程

```
需求分析
    ↓
设计
    ↓
编码
    ↓
测试
    ↓
部署
    ↓
维护
```

### 敏捷开发

**定义**：迭代式开发方法

**特点：**
- 小步快跑
- 持续交付
- 拥抱变化

### TDD（测试驱动开发）

**定义**：先写测试，再写代码

**流程：**
1. 写测试（失败）
2. 写代码（通过）
3. 重构

---

## 📊 算法和数据结构

### 算法

**定义**：解决问题的步骤

**常见算法：**
- **排序**：冒泡、快速排序、归并排序
- **查找**：线性查找、二分查找
- **图算法**：DFS、BFS、最短路径
- **动态规划**

### 数据结构

**定义**：组织和存储数据的方式

**常见数据结构：**
- **线性**：数组、链表、栈、队列
- **非线性**：树、图、堆
- **键值对**：哈希表、字典

### 时间复杂度

**定义**：算法运行时间随数据规模增长的快慢

**常见复杂度：**
- **O(1)** - 常数时间
- **O(log n)** - 对数时间
- **O(n)** - 线性时间
- **O(n log n)** - 线性对数时间
- **O(n²)** - 平方时间

---

## 🚀 实践示例

### 使用函数封装重复代码

**不好的代码：**
```python
print("计算3 + 5")
result1 = 3 + 5

print("计算10 + 20")
result2 = 10 + 20

print("计算7 + 8")
result3 = 7 + 8
```

**好的代码：**
```python
def add(a, b):
    return a + b

result1 = add(3, 5)
result2 = add(10, 20)
result3 = add(7, 8)
```

### 使用数据结构组织数据

**场景：存储学生信息

**使用列表：**
```python
students = ["张三", 20, "李四", 21]  # 不清楚
```

**使用字典：**
```python
student = {
    "name": "张三",
    "age": 20,
    "major": "计算机"
}  # 清晰明了
```

---

## 📚 学习资源

### 在线教程
- [菜鸟教程 - 编程术语](https://www.runoob.com/glossary.html)
- [维基百科 - 计算机科学](https://zh.wikipedia.org/wiki/计算机科学)

### 书籍推荐
- 《代码大全》
- 《程序员修炼之道》
- 《设计模式》

---

## ❓ 常见问题

### Q1: 函数和方法有什么区别？
**A**:
- **函数**：独立的代码块
- **方法**：属于类或对象的函数
- **关系**：方法是函数的子集

### Q2: 面向对象难学吗？
**A**:
- **概念不难**：封装、继承、多态
- **需要实践**：多写代码理解
- **不要急于求成**：先会使用，再深入原理

### Q3: 一定要学设计模式吗？
**A**:
- **新手**：不需要，先打好基础
- **进阶**：必要，提高代码质量
- **原则**：理解思想，不要死记

### Q4: 算法需要学到什么程度？
**A**:
- **Web开发**：基础算法够用
- **算法竞赛**：深入掌握
- **AI方向**：需要数学和算法基础

---

## 💡 学习建议

1. **理解核心概念**
   - 变量、函数、循环
   - 面向对象思想
   - 数据结构

2. **多实践**
   - 写代码理解概念
   - 做小项目巩固
   - 阅读优秀代码

3. **循序渐进**
   - 不要一次学太多
   - 先会用，再深入
   - 慢慢积累

4. **查阅文档**
   - 遇到不懂的术语
   - 查官方文档
   - 建立知识体系

---

**下一步**：
- [常见缩写速查表](/知识扫盲/常见缩写速查表.md)
- [计算机基础](/知识扫盲/计算机基础.md)
